# WebSocket URL for village.cx connection (required)
WS_URL=wss://village.cx/ws

# Base URL for village.cx site (optional, defaults to https://village.cx)
BASE_URL=https://village.cx

# Language locale (optional, defaults to en)
LOCALE=en
